import React from "react";
import ReactDOM from "react-dom/client";
import CustomerDisplay from "@/domains/pos/pages/CustomerDisplay";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
	// <React.StrictMode>
	<CustomerDisplay />
	/* </React.StrictMode> */
);
