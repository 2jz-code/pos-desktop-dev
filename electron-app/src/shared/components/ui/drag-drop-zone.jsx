
import React from 'react';

const DragDropZone = ({ onDrop, acceptTypes, showPreview, className, children }) => {
  return (
    <div className={className}>
      {children}
      {/* Implementation coming soon */}
    </div>
  );
};

export default DragDropZone;
